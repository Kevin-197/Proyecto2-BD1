
/*const mysql = require('mysql');

const mysqlConnection = mysql.createConnection({
    host: 'mysql_bases1',
    user: 'root',
    password: '123456',
    database: 'ChessWorldWide'
});

mysqlConnection.connect( (err:any) => {
    if (err){
        console.log(err);
        return;
    }
    else {
        console.log("Db connected");
    }
});
export {mysqlConnection as bdConnection};
*/